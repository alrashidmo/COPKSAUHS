# ============================================================
# STUDENT PORTAL API - ENVIRONMENT VARIABLES
# ============================================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=student_portal

# ============================================================
# SETUP INSTRUCTIONS:
# 1. Update DB_USER and DB_PASSWORD with your MySQL credentials
# 2. Update DB_HOST if your database is on a different server
# 3. Save this file as .env in the api/ directory
# ============================================================
